{
  "name": "project",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf ./dist && rollup --config"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "rollup": "^4.12.0",
    "rollup-plugin-esbuild": "^6.1.1",
    "rollup-plugin-typescript-paths": "^1.5.0",
    "typescript": "^5.3.3"
  }
}
